{# {% extends 'base.html.twig' %}

{% block title %}Partenaire index{% endblock %}

{% block body %}
    <h1>Partenaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Societe</th>
                <th>Siret</th>
                <th>Metier</th>
                <th>Telephone</th>
                <th>EmailContact</th>
                <th>Departement</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>DeletedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partenaire in partenaires %}
            <tr>
                <td>{{ partenaire.id }}</td>
                <td>{{ partenaire.nom }}</td>
                <td>{{ partenaire.prenom }}</td>
                <td>{{ partenaire.societe }}</td>
                <td>{{ partenaire.siret }}</td>
                <td>{{ partenaire.metier }}</td>
                <td>{{ partenaire.telephone }}</td>
                <td>{{ partenaire.emailContact }}</td>
                <td>{{ partenaire.departement }}</td>
                <td>{{ partenaire.createdAt ? partenaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ partenaire.updatedAt ? partenaire.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ partenaire.deletedAt ? partenaire.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_partenaire_show', {'id': partenaire.id}) }}">show</a>
                    <a href="{{ path('app_partenaire_edit', {'id': partenaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="13">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_partenaire_new') }}">Create new</a>
{% endblock %} #}
